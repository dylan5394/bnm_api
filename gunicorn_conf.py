bind = "0.0.0.0:8000"

# errorlog = '-'
# loglevel = 'info'
# accesslog = '-'
# access_log_format = '%(h)s %(t)s "%(r)s" %(s)s "%(f)s" "%(a)s" "%({HTTP_AUTHORIZATION}i)s"'
#
#
# def pre_request(worker, req):
#     worker.log.info("%s %s %s" % (req.method, req.path, req.headers))
